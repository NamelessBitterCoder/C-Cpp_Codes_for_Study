clc;clear;

% 分子系数
b = [1, -1/4, 0];
% 分母系数
a = [1, -8/5, 1/15];

% 部分分式展开
[r, p, c] = residue(b, a);

% 输出留数、极点和直接项
fprintf('留数 r = \n');
disp(r);
fprintf('极点 p = \n');
disp(p);
fprintf('直接项 c = \n');
disp(c);

fprintf('z反变换结果：x(n) = %.4f*(1/3)^n u(-n-1) + %.4f*(1/5)')